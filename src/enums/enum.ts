/**
 * LogType enum
 * @description Enum options to use for LogDebugger class
 * @method log() @class LogDebugger
 */
export enum LogType {
  INFO = 'info',
  SUCCESS = 'success',
  ERROR = 'error',
  WARNING = 'warning',
  DEBUG = 'debug',
}
